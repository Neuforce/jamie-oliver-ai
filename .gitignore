node_modules/
.venv/
venv/
*.so
*.dylib
*.node
*.pyc
.env
dist/
build/
.DS_Store
.vercel
.env*.local
logs/
apps/frontend/package-lock.jsonapps/backend-search/data/media/
.github/pull_request_template_filled.md

# Linear API key para el agente (opcional)
.cursor/environment.json

# Linear API key para scripts (scripts/linear.sh)
scripts/.env

# Local design-reference mocks (Figma exports + HTML prototypes)
# Kept out of source control so the repo stays lean; browse locally.
jamie-ui-components/

# Scratch / throwaway dev artifacts
git-status.txt
